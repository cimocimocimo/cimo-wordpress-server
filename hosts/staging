# Add each host to the [staging] group and to a "type" group such as [web] or [db].
# List each machine only once per [group], even if it will host multiple sites.

[staging]
52.72.96.100

[staging:vars]
# ansible_ssh_private_key_file=~/.ssh/cimo-wordpress.pem

[web]
52.72.96.100

[web:vars]
ansible_ssh_private_key_file=~/.ssh/cimo-wordpress-server-web